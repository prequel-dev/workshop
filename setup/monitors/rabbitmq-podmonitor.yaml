apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: rabbitmq
  namespace: rabbitmq
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: import-definitions-v4
  namespaceSelector:
    matchNames:
    - rabbitmq
  podMetricsEndpoints:
  - port: prometheus
    interval: 30s
    path: /metrics
